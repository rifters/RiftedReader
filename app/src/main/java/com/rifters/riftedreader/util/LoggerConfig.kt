package com.rifters.riftedreader.util

/**
 * Configuration for the AppLogger
 */
object LoggerConfig {
    var enableFileLogging = true
    var enableLogcat = true
    var defaultLogFile = "session_log.txt"
    var tag = "AppLogger"
}
